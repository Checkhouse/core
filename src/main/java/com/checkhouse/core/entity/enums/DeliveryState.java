package com.checkhouse.core.entity.enums;

// TODO: State관련 ENUM 정리
public enum DeliveryState {
    PENDING, PROCESSING, SHIPPED, IN_TRANSIT, OUT_FOR_DELIVERY, DELIVERED, FAILED, RETURNED, CANCELED,
}